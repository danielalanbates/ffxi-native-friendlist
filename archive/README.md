# Archive

`2026-09-12-harness-experiments/` holds the throwaway payloads run through the harness `LUA` channel
while reverse engineering. They're superseded by `addon/nativefriends`, but they record how the
hook, the category probe (screenshot 02), and the in-game install copy were first proven. They
contain hardcoded addresses for one client build, so don't run them against anything else.
